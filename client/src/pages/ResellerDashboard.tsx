import { useEffect, useState } from "react";
import { ArrowRight, CheckCircle2, Clock3, Package, ShoppingBag, WalletCards } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { calculateCommission, formatCurrency, getStore, statusLabel, subscribeStore, updateStore, type LocalUser, type OrderStatus } from "@/lib/localStore";
import type { Section } from "@/components/AppShell";

type Props = { user: LocalUser; section: Section };
export default function ResellerDashboard({ user, section }: Props) { const [, setVersion] = useState(0); useEffect(() => subscribeStore(() => setVersion(value => value + 1)), []); const store = getStore(); const ownOrders = store.orders.filter(order => order.resellerId === user.id); if (section === "catalogo") return <ResellerCatalog user={user} />; if (section === "pedidos") return <ResellerOrders orders={ownOrders} />; if (section === "comissoes") return <ResellerCommissions orders={ownOrders} />; const total = ownOrders.reduce((sum, order) => sum + order.total, 0); const commissions = ownOrders.reduce((sum, order) => sum + order.commission, 0); return <><Intro title={`Olá, ${user.name.split(" ")[0]}.`} text="Aqui está um resumo da sua jornada com a Fernanda Fortes." /><div className="grid gap-4 sm:grid-cols-3"><Metric icon={ShoppingBag} label="Pedidos realizados" value={String(ownOrders.length)} /><Metric icon={WalletCards} label="Comissões acumuladas" value={formatCurrency(commissions)} /><Metric icon={Package} label="Vendas no período" value={formatCurrency(total)} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><div className="rounded-2xl border border-[#dfd4c3] bg-[#fbf8f3] p-6"><div className="flex items-center justify-between"><div><p className="eyebrow text-[#9d7d48]">Acompanhe de perto</p><h3 className="serif mt-2 text-2xl">Seus pedidos recentes</h3></div><button className="text-sm text-[#846737]" onClick={() => window.dispatchEvent(new CustomEvent("reseller-go-orders"))}>Ver todos</button></div><div className="mt-6 space-y-3">{ownOrders.slice(0, 3).map(order => <OrderRow key={order.id} order={order} />)}</div></div><div className="rounded-2xl border border-[#dfd4c3] bg-[#eee4d3] p-6"><p className="eyebrow text-[#9d7d48]">Para você</p><h3 className="serif mt-2 text-2xl">Descubra novas peças</h3><p className="mt-3 text-sm leading-6 text-[#69756b]">Explore o catálogo e escolha as peças que mais combinam com as suas clientes.</p><Button className="mt-7 rounded-xl bg-[#263b32] text-[#f8f4ed] hover:bg-[#344e42]" onClick={() => window.dispatchEvent(new CustomEvent("reseller-go-catalog"))}>Ver catálogo <ArrowRight className="ml-2 h-4 w-4" /></Button></div></div></> }
function Intro({ title, text }: { title: string; text: string }) { return <div className="mb-8"><p className="eyebrow text-[#9d7d48]">Meu espaço</p><h2 className="serif mt-2 text-4xl">{title}</h2><p className="mt-3 text-sm text-[#69756b]">{text}</p></div> }
function Metric({ icon: Icon, label, value }: { icon: typeof ShoppingBag; label: string; value: string }) { return <div className="rounded-2xl border border-[#dfd4c3] bg-[#fbf8f3] p-5"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#eee4d3] text-[#896b3b]"><Icon className="h-5 w-5" /></div><p className="mt-7 text-xs uppercase tracking-[.12em] text-[#899086]">{label}</p><p className="serif mt-1 text-3xl">{value}</p></div> }
function OrderRow({ order }: { order: ReturnType<typeof getStore>["orders"][number] }) { return <div className="flex items-center justify-between rounded-xl border border-[#e8dfd2] bg-[#f8f4ed] p-4"><div><p className="text-sm font-medium">{order.id}</p><p className="mt-1 text-xs text-[#8b9187]">{new Date(order.createdAt).toLocaleDateString("pt-BR")}</p></div><div className="text-right"><p className="text-sm font-medium">{formatCurrency(order.total)}</p><Badge className="mt-1 bg-[#e2eee4] text-xs text-[#58705d]">{statusLabel[order.status]}</Badge></div></div> }
function ResellerCatalog({ user }: { user: LocalUser }) { const [cart, setCart] = useState<Record<string, number>>({}); const store = getStore(); const selected = Object.entries(cart).filter(([, quantity]) => quantity > 0); const total = selected.reduce((sum, [id, quantity]) => sum + (store.products.find(product => product.id === id)?.price ?? 0) * quantity, 0); return <><Intro title="Escolha suas peças" text="Monte pedidos com o catálogo disponível da sua gestora." /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{store.products.map(product => <div key={product.id} className="overflow-hidden rounded-2xl border border-[#dfd4c3] bg-[#fbf8f3]"><div className="flex h-36 items-center justify-center bg-[#eee4d3]"><div className="h-16 w-16 rotate-45 border border-[#b8955b]" /></div><div className="p-5"><p className="text-xs uppercase tracking-[.1em] text-[#9d7d48]">{product.category}</p><h3 className="serif mt-2 text-2xl">{product.name}</h3><div className="mt-4 flex items-center justify-between"><span className="font-medium">{formatCurrency(product.price)}</span><span className="text-xs text-[#8b9187]">{product.stock} disponíveis</span></div><Button disabled={product.stock === 0} onClick={() => setCart(current => ({ ...current, [product.id]: (current[product.id] ?? 0) + 1 }))} className="mt-5 h-10 w-full rounded-xl bg-[#263b32] text-[#f8f4ed] hover:bg-[#344e42] disabled:bg-[#d1cbc0]">{product.stock === 0 ? "Indisponível" : cart[product.id] ? `Adicionar mais (${cart[product.id]})` : "Adicionar ao pedido"}</Button></div></div>)}</div>{selected.length > 0 && <div className="sticky bottom-5 mt-6 flex items-center justify-between rounded-2xl bg-[#263b32] p-4 text-[#f8f4ed] shadow-xl"><div><p className="text-sm">{selected.reduce((sum, [, quantity]) => sum + quantity, 0)} item(ns) selecionado(s)</p><p className="mt-1 text-xs text-[#c2cec3]">Total estimado: {formatCurrency(total)}</p></div><Button onClick={() => { updateStore(next => { const orderTotal = selected.reduce((sum, [id, quantity]) => sum + (next.products.find(product => product.id === id)?.price ?? 0) * quantity, 0); next.orders.unshift({ id: `PED-${Math.floor(1000 + Math.random() * 8999)}`, resellerId: user.id, items: selected.map(([productId, quantity]) => ({ productId, quantity })), total: orderTotal, commission: calculateCommission(orderTotal, user.commissionRate), status: "pending", createdAt: new Date().toISOString() }); next.notifications.unshift({ id: crypto.randomUUID(), title: "Pedido enviado", message: "Seu pedido foi enviado para aprovação.", read: false, createdAt: new Date().toISOString() }); }); setCart({}); window.alert("Pedido enviado para aprovação."); }} className="rounded-xl bg-[#e4c98f] text-[#263b32] hover:bg-[#d8ba79]">Enviar pedido <ArrowRight className="ml-2 h-4 w-4" /></Button></div>}</> }
function ResellerOrders({ orders }: { orders: ReturnType<typeof getStore>["orders"] }) { return <><Intro title="Meus pedidos" text="Acompanhe o status e o histórico de tudo o que você vendeu." /><div className="space-y-3">{orders.map(order => <div key={order.id} className="rounded-2xl border border-[#dfd4c3] bg-[#fbf8f3] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="font-medium">{order.id}</p><p className="mt-1 text-xs text-[#8b9187]">Criado em {new Date(order.createdAt).toLocaleDateString("pt-BR")}</p></div><div className="flex items-center gap-4"><span className="font-medium">{formatCurrency(order.total)}</span><Badge className="bg-[#e2eee4] text-[#58705d]">{statusLabel[order.status]}</Badge></div></div><div className="mt-5 flex items-center gap-1">{(["pending", "approved", "shipped", "delivered"] as OrderStatus[]).map((status, index) => <div key={status} className="flex flex-1 items-center gap-1"><div className={`h-1.5 w-full rounded-full ${["pending", "approved", "shipped", "delivered"].indexOf(order.status) >= index ? "bg-[#b8955b]" : "bg-[#e6dfd4]"}`} />{index < 3 && <div className="hidden h-1 w-1 rounded-full bg-[#d8ccba] sm:block" />}</div>)}</div></div>)}</div></> }
function ResellerCommissions({ orders }: { orders: ReturnType<typeof getStore>["orders"] }) { const total = orders.reduce((sum, order) => sum + order.commission, 0); return <><Intro title="Minhas comissões" text="Seu extrato de ganhos, sempre claro e atualizado." /><div className="rounded-3xl bg-[#263b32] p-7 text-[#f8f4ed] md:p-9"><p className="eyebrow text-[#dfc58f]">Total acumulado</p><p className="serif mt-3 text-5xl">{formatCurrency(total)}</p><p className="mt-3 text-sm text-[#c2cec3]">A partir de {orders.length} pedido(s) registrados.</p></div><div className="mt-6 overflow-hidden rounded-2xl border border-[#dfd4c3] bg-[#fbf8f3]"><div className="border-b border-[#e5dbcc] px-5 py-4 font-medium">Extrato</div>{orders.map(order => <div key={order.id} className="flex items-center justify-between border-b border-[#eee7dd] px-5 py-5 last:border-0"><div><p className="text-sm font-medium">{order.id}</p><p className="mt-1 text-xs text-[#8c9187]">{new Date(order.createdAt).toLocaleDateString("pt-BR")}</p></div><p className="text-sm font-medium text-[#58705d]">+ {formatCurrency(order.commission)}</p></div>)}</div></> }
